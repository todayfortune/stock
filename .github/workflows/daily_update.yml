name: Daily MSI Data Collection

on:
  schedule:
    # 매일 한국시간 15:50 (UTC 06:50) 실행
    - cron: '50 6 * * 1-5'
  workflow_dispatch:      # 수동 실행 버튼

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 저장소 체크아웃 (코드 가져오기)
      uses: actions/checkout@v3

    - name: 파이썬 설정 (3.9 버전)
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: 라이브러리 설치 (yfinance 포함 필수 도구 설치)
      run: |
        pip install finance-datareader yfinance pandas numpy requests lxml

    - name: MSI 수집기 실행 (데이터 긁어오기)
      run: |
        python scripts/fetch_krx.py

    - name: 결과 저장 (Commit & Push)
      run: |
        git config --global user.name "MSI-Bot"
        git config --global user.email "actions@github.com"
        git add data/*.json
        git commit -m "📈 Update MSI Data" || exit 0
        git push
